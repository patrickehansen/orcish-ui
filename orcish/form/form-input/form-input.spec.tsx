import React from 'react';
import { render } from '@testing-library/react';
import { BasicFormInput } from './form-input.composition';

it('should render with the correct text', () => {
  // const { getByText } = render(<BasicFormInput />);
  // const rendered = getByText('hello from FormInput');
  expect(true).toBeTruthy();
});
